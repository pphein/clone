var app = new Vue({ el: "#app", methods: { moment: function (date) { return moment(date); } }, data: function () { return { search: "", jobs: window.jobs }; }, computed: { filteredJobs: function () { var self = this; return this.jobs.filter(function (job) { return (job.company.name.toLowerCase().indexOf(self.search.toLowerCase()) >= 0 || job.title.toLowerCase().indexOf(self.search.toLowerCase()) >= 0); }); } } });